# 2026-07-28 21:00 学习班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；本轮严禁触碰 `/home/xujiaz/xiaoguang-blog` 及其 08:30 选题任务。
- Git 状态：开始时本地 `main` 与 `origin/main` 同步在 `1d7f557f3deef054dc2c7955db5d5c3281f32a02`，无未提交业务改动。执行 `npm run review:site` 后仅 `ops/site-review.json` 被刷新，这是本轮维护产生的记录文件变更。
- GitHub Pages：最近 5 次 `Deploy AI Frontier Atlas` workflow 均为 success，最近一次成功部署为 `docs: record cosmos signal deployment`。
- 当前站点结构：首页是 AI 研究工作台，前沿页是信号矩阵，学习页公开四阶段课程地图，方法页明确来源分级、四维评分与回顾优先流程。
- 当前数据概况：`frontier.json` 有 5 条 S 级前沿信号，最新日期为 2026-07-27；`tracks.json` 有 6 条研究路线；`syllabus.json` 有 46 节规划；`lessons.json` 和 `articles.json` 有 7 节完整课程；`ops/site-review.json` 显示 30 个直接引用。
- 最近更新：2026-07-27 21:00 新增《Transformer：注意力的张量与复杂度》并部署成功；2026-07-28 09:00 新增 `nvidia-cosmos-h-dreams-surgical-world-model` 前沿信号并部署成功。
- 重复或陈旧主题：前沿信号仍偏开放模型、MoE、训练基础设施和模型经济性，Kimi K3 与 Instella-MoE 都带 MoE/开放栈属性；学习内容中基础模型已有 MoE 与注意力两课，继续补基础会加重单一路线倾斜。
- 空路线与薄弱路线：六条路线都已有至少 1 节课程，不存在完全空路线；多模态、训练、推理、Agent、评测均为 1/N。多模态路线只有 CLIP，而上午新增 Physical AI / world model 信号后，视觉 token 是连接图文表示、跨模态适配器、扩散模型和世界模型理解的共同前置知识。
- 待复查来源：Kimi K3 仍待完整技术报告、权重与推理代码；Claude Opus 5 仍需第三方同预算任务评测；Instella-MoE 需复查跨硬件复现；llm-d 需复查公开基准、快照开销与多集群稳定性；Cosmos-H-Dreams 需继续复查技术报告是否从 TODO 变为公开。

## 本轮班次与目标

- 班次：21:00 学习班次。
- 目标：根据 trackCoverage 和知识依赖推进 1 节完整课程，选择多模态路线第 2 课《视觉 Token：ViT 与图像离散化》。

## 计划动作和选择理由

1. 核对 ViT 原始论文、DINOv2 官方论文/代码、MAE 原始论文及图像离散化相关一手来源，确保能解释 patch embedding、位置编码、token 序列、mask 重建和离散视觉 token 的差异。
2. 在 `src/data/syllabus.json` 中为多模态第 2 课补 slug，并将状态从 `planned` 改为 `available`，不改变后续课程顺序。
3. 在 `src/data/lessons.json` 新增结构化课程，覆盖学习目标、先修、概念地图、代码脉络、张量路径、复杂度、工程决策、失败边界、动手实验、自检问题和至少 3 个可靠来源。
4. 在 `src/data/articles.json` 新增对应长文，正文不少于 1800 个中文字符，包含至少 2 个公式、最小例子、源码路径、工程意义和原始来源。
5. 按维护手册完整验证链测试；任何失败都不提交、不推送。

## 明确不做

- 不新增 09:00 前沿信号，不追逐热搜。
- 不修改 `/home/xujiaz/xiaoguang-blog`、其自动任务或旧技术博客内容。
- 不重做视觉风格，不改动与课程发布无关的页面、组件或样式。
- 不提前发布跨模态连接器、扩散模型或多模态评测课程；本轮只补视觉 token 基础。

## 来源与验收标准

- 来源优先级：ViT 原始论文；MAE 原始论文；DINOv2 官方论文和 Meta 官方代码；必要时补充 VQ-VAE / BEiT 原始论文解释离散视觉 token。
- 内容验收：必须清楚区分 patch token、class token、position embedding、masked image modeling、离散 token/codebook 与连续 patch embedding；必须包含概念地图、代码脉络、工程意义、自检问题和原始来源。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许提交推送。

## 实际执行

- 按维护手册先完成回顾与计划，再开始编辑 `src/data/` 与脚本文件。
- 核对并采用 5 个一手/可靠来源：ViT 原始论文、MAE 原始论文、DINOv2 原始论文、Meta Research DINOv2 官方源码、BEiT 原始论文。
- 将 `src/data/syllabus.json` 中多模态第 2 课改为 `available`，slug 为 `vision-tokens-vit-image-tokenization`，未改变后续课程顺序。
- 在 `src/data/lessons.json` 新增《视觉 Token：ViT 与图像离散化》，覆盖学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、决策表、动手实验、自检问题和 5 个来源。
- 在 `src/data/articles.json` 新增对应研究长文：7 章连续解释、约 2700 个中文字符、3 个公式、最小 token 预算例子、实现映射、4 条失败边界和 4 条结论。
- 更新 `scripts/verify-build.mjs`，改为从 `src/data/lessons.json` 动态验证全部已发布课程页，避免后续新增课程时遗漏构建检查。
- 运行 `npm run review:site` 后刷新 `ops/site-review.json`：前沿信号保持 5 条，课程从 7 节增至 8 节，直接引用从 30 个增至 35 个。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，站点回顾更新为 5 条前沿信号、8 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 13 个静态页面，包含新增课程页。
- `npm run verify:build`：12/12 页面验证通过，动态覆盖首页、分区页和 8 个课程页。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。

## 发布结果

- 内容提交 `d95d1fe`（`feat: add vision token lesson`）已推送 `main`。
- GitHub Actions `30362165806` 成功：build job 20s，deploy job 8s。备注：Actions 输出 Node 20 deprecation annotation，但构建与部署均成功。
- 线上首页 `https://sunshine0912-love.github.io/ai-frontier-atlas/` 返回 200，并检测到“AI 前沿研究”和“DEEP LESSONS”。
- 线上前沿页 `https://sunshine0912-love.github.io/ai-frontier-atlas/frontier/` 返回 200，并检测到“信号矩阵”和 Cosmos-H-Dreams 信号。
- 线上学习页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/` 返回 200，并检测到《视觉 Token：ViT 与图像离散化》。
- 新增课程页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/vision-tokens-vit-image-tokenization/` 返回 200，并检测到《视觉 Token：ViT 与图像离散化》、“Patch token 数量”和“Meta Research DINOv2 官方源码”。
- 线上方法页 `https://sunshine0912-love.github.io/ai-frontier-atlas/method/` 返回 200，并检测到“来源、判断”和“四维评分”。

## 下一方向

- 下一轮学习班次优先补训练路线第 2 课《预训练目标：因果语言建模与数据混合》，因为多模态已补到 2/6，训练仍只有偏好对齐一课，且预训练目标是优化器、数据并行、SFT 和训练诊断的共同先修。
- 下一轮 09:00 前沿班次继续优先寻找非 MoE 类信号，尤其是评测、安全、推理系统、可复现开放训练栈和 Cosmos-H-Dreams 技术报告公开状态。
