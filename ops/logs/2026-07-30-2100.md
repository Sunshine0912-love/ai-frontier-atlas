# 2026-07-30 21:00 学习班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；本轮严禁修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`，也不改动其 08:30 选题任务。
- Git 状态：开始时本地 `main` 与 `origin/main` 同步，无未提交业务改动；远端 `origin/main` 为 `f3d26c1e532b26b3c2df5b8bd5b4cb2b6a211efc`。执行 `npm run review:site` 后仅刷新 `ops/site-review.json`，这是本轮维护产生的记录文件变更。
- GitHub Pages：Pages 使用 workflow 部署，公开地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；最近 5 次 `Deploy AI Frontier Atlas` workflow 均为 success，最近一次成功部署为 `docs: record arc agi signal deployment`。
- 当前站点结构：首页是 AI 研究工作台，前沿页是信号矩阵，学习页公开四阶段课程地图，方法页明确来源分级、四维评分与回顾优先流程。
- 当前数据概况：`frontier.json` 有 7 条 S 级前沿信号，最新日期为 2026-07-29；`tracks.json` 有 6 条研究路线；`syllabus.json` 有 46 节规划；`lessons.json` 和 `articles.json` 有 9 节完整课程；`ops/site-review.json` 显示 50 个直接引用。
- 最近更新：2026-07-30 09:00 新增 `openai-arc-agi-3-retained-reasoning-compaction` 前沿信号并部署成功；2026-07-29 21:00 新增《预训练目标：因果语言建模与数据混合》课程并部署成功。
- 重复或陈旧主题：前沿信号中 Kimi K3 与 Instella-MoE 都有 MoE/开放栈属性，NOOA 与 ARC-AGI-3 都指向 Agent harness/评测，Cosmos-H-Dreams 仍等待技术报告补齐；今晚是学习班次，不新增前沿信号，也不继续加重 MoE 与 Agent 主题。
- 空路线与薄弱路线：六条路线都已有至少 1 节课程，不存在完全空路线；按 `trackCoverage`，推理、Agent、评测均为 1/N，基础、多模态、训练为 2/N。推理路线第 2 课是后续 FlashAttention、量化、投机解码、并行推理和容量规划的共同先修，应优先补齐。
- 待复查来源：Kimi K3 仍待完整技术报告、权重与推理代码；Claude Opus 5 仍需第三方同预算任务评测；Instella-MoE 需复查技术报告与跨硬件复现；llm-d 需复查公开基准、快照开销与多集群稳定性；Cosmos-H-Dreams 需复查技术报告是否从 TODO 变为公开；NOOA 和 ARC-AGI-3 需跟踪第三方复现与 harness 标注规范。

## 本轮班次与目标

- 班次：21:00 学习班次。
- 目标：根据 `trackCoverage` 和课程依赖推进 1 节完整课程，选择推理路线第 2 课《推理性能模型：TTFT、TPOT 与吞吐》。

## 计划动作和选择理由

1. 核对推理 serving 的一手来源：优先 vLLM/PagedAttention、Orca iteration-level scheduling、FlashAttention 或相关官方源码/文档，以及供应商对 TTFT/TPOT/吞吐指标的工程定义。
2. 在 `src/data/syllabus.json` 中为推理路线第 2 课补 slug，并将状态从 `planned` 改为 `available`，不改变后续课程顺序。
3. 在 `src/data/lessons.json` 新增结构化课程，覆盖学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、决策表、动手实验、自检问题和至少 3 个可靠来源。
4. 在 `src/data/articles.json` 新增对应长文，正文不少于 1800 个中文字符，包含至少 2 个公式、最小例子、源码路径、复杂度或性能分析、工程意义和原始来源。
5. 按维护手册完整验证链测试；任何失败都不提交、不推送。

## 明确不做

- 不新增 09:00 前沿信号，不追逐热搜。
- 不修改 `/home/xujiaz/xiaoguang-blog`、其自动任务或旧技术博客内容。
- 不重做视觉风格，不改动与课程发布无关的页面、组件或样式。
- 不提前发布 FlashAttention、量化、投机解码或容量规划课程；本轮只建立推理性能指标和容量模型基础。

## 来源与验收标准

- 来源优先级：PagedAttention/vLLM 原始论文与官方源码/文档；Orca iteration-level scheduling 论文；Triton/TensorRT-LLM/vLLM 等官方 serving 文档；必要时补充 Hugging Face TGI 或 NVIDIA 性能文档解释指标口径。
- 内容验收：必须包含概念地图、代码脉络、工程意义、自检问题和原始来源；必须明确区分 TTFT、TPOT、end-to-end latency、throughput、goodput、prefill、decode、queueing、KV cache 和 SLO。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许提交推送。

## 实际执行

- 已按维护手册先完成回顾与计划，再开始编辑 `src/data/`。
- 使用 agent-reach 的 web/dev 路由核对来源：PagedAttention/vLLM arXiv API 返回原始论文摘要与源码地址；USENIX OSDI 22 页面返回 Orca 论文摘要、PDF 与 BibTeX；vLLM GitHub 官方仓库、metrics 设计文档、benchmark CLI 文档和 v1 metrics 源码均可访问。
- 将 `src/data/syllabus.json` 中推理路线第 2 课改为 `available`，slug 为 `inference-performance-ttft-tpot-throughput`，未改变后续课程顺序。
- 在 `src/data/lessons.json` 新增《推理性能模型：TTFT、TPOT 与吞吐》，覆盖学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、决策表、动手实验、自检问题和 5 个来源。
- 在 `src/data/articles.json` 新增对应研究长文：8 章连续解释、3 个公式、混合负载最小例子、实现映射、4 条失败边界和 4 条结论；正文约 4200 个中文字符。
- 首次 `npm test` 发现第一条公式解释不足 30 字，已补充为更完整的 SLO 判断；首次 `npm run build` 发现 `\frac` 等 LaTeX 反斜杠被 JSON 转义吞掉，已修复为合法 KaTeX 字符串，并从 `npm test` 重新跑完整验证链。
- 运行 `npm run review:site` 后刷新 `ops/site-review.json`：前沿信号保持 7 条，课程从 9 节增至 10 节，直接引用从 50 个增至 55 个，推理路线覆盖率从 1/8 提升到 2/8。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，站点回顾更新为 7 条前沿信号、10 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 15 个静态页面，包含新增课程页。
- `npm run verify:build`：14/14 页面验证通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。
- 直接来源可访问性：arXiv PagedAttention、USENIX Orca、vLLM metrics 设计文档、vLLM benchmark CLI 文档和 vLLM metrics 源码均返回 200；vLLM benchmark CLI 由 `.html` 地址 302 到规范路径后返回 200。

## 发布结果

- 内容提交 `e84d4d9`（`feat: add inference performance lesson`）已推送 `main`。
- GitHub Actions `30546042111` 成功：build job 29s，deploy job 10s。备注：Actions 输出 Node 20 deprecation annotation，但构建与部署均成功。
- 线上首页 `https://sunshine0912-love.github.io/ai-frontier-atlas/` 返回 200，并检测到 “DEEP LESSONS” 计数为 10、“PRIMARY REFERENCES” 计数为 55。
- 线上学习页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/` 返回 200，并检测到《推理性能模型：TTFT、TPOT 与吞吐》位于推理与服务路线。
- 新增课程页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/inference-performance-ttft-tpot-throughput/` 返回 200，并检测到《推理性能模型：TTFT、TPOT 与吞吐》、“平均 TPOT”、“SLO Goodput”和 vLLM Metrics 设计文档来源。
- 线上前沿页 `https://sunshine0912-love.github.io/ai-frontier-atlas/frontier/` 返回 200，并检测到“信号矩阵”和 ARC-AGI-3 信号。
- 线上方法页 `https://sunshine0912-love.github.io/ai-frontier-atlas/method/` 返回 200，并检测到“来源、判断”和“四维评分方法”。
- 本课 5 个直接来源均已核对可访问：arXiv PagedAttention、USENIX Orca、vLLM metrics 设计文档、vLLM benchmark CLI 文档和 vLLM v1 metrics 源码。

## 下一方向

- 下一轮 21:00 学习班次优先在 Agent 或评测路线中补第 2 课，因为推理路线已补到 2/8，而 Agent 与评测仍为 1/N；若按依赖选择，Agent 第 2 课《工具协议：Schema、权限与错误语义》可承接已有 Agent 状态课和 ARC-AGI-3/NOOA 前沿信号。
- 下一轮 09:00 前沿班次继续复查非 MoE 类信号，重点关注推理系统、Agent 评测/安全、训练数据治理，以及 Kimi K3 技术报告/权重、Instella-MoE 技术报告、Cosmos-H-Dreams 技术报告和 NOOA 第三方复现。
