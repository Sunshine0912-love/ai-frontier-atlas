# 2026-07-27 21:00 学习班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；未触碰旧技术博客。
- Git 状态：本地 `main` 与 `origin/main` 同步在 `18a1c6b`；开始时无未提交业务改动。`npm run review:site` 按维护手册刷新了 `ops/site-review.json` 的 `generatedAt`。
- GitHub Pages：Pages 源为 `main /`，最近三次 GitHub Actions 部署均为 success。
- 当前站点：4 条前沿信号、6 条研究路线、6 节完整课程、46 节 syllabus 规划、22 个直接引用；首页、前沿、学习、方法页结构稳定。
- 最近更新：15:43 完成研究工作台与课程模型重构；16:10 完成可读性、KaTeX、本地公式渲染、完整 syllabus 与六篇研究级长文。
- 重复或陈旧主题：前沿信号集中在开放模型、MoE、训练基础设施和模型经济性，Kimi K3 与 Instella-MoE 都围绕 MoE/开放栈，需要后续避免继续堆叠同类发布；最新前沿日期为 2026-07-24，晚间班次不追热点。
- 空路线：六条路线都已有第 1 课，不存在完全空路线；但所有路线覆盖率都仍为 1/N，基础模型第 2 课是后续位置表示、长上下文、推理性能、FlashAttention 等课程的共同依赖。
- 待复查来源：Kimi K3 与 Claude Opus 5 仍主要依赖官方发布，需等权重、技术报告或第三方同预算评测；Instella-MoE 需复查完整技术报告与跨硬件复现；llm-d 需复查公开基准、快照开销和多集群稳定性。

## 本轮班次与目标

- 班次：21:00 学习班次。
- 目标：按 trackCoverage 和知识依赖推进 1 节完整课程，优先补基础模型路线的第 2 课《Transformer：注意力的张量与复杂度》。

## 计划动作和选择理由

1. 研究并核对 Transformer 注意力的一手来源：原始论文、PyTorch SDPA 官方文档/源码、FlashAttention 论文或源码作为工程边界补充。
2. 在 `src/data/syllabus.json` 中将基础模型第 2 课从 `planned` 改为 `available` 并补 slug。
3. 在 `src/data/lessons.json` 新增第 2 课结构化课程，包含学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、实验、自检问题和至少 3 个可靠来源。
4. 在 `src/data/articles.json` 新增对应长文，保持不少于 1800 个中文字符、至少 2 个公式、原始来源、实现映射、失败边界和工程判断。
5. 运行维护手册要求的完整验证链，全部通过后提交并推送。

## 明确不做

- 不新增 09:00 前沿信号，不追逐社交热搜。
- 不修改 `/home/xujiaz/xiaoguang-blog`、其自动任务或旧技术博客内容。
- 不重做视觉风格、不改动与本课无关的组件和样式。
- 不把 FlashAttention 课程提前发布；本轮只在注意力基础课中解释 IO 边界，详细课程仍留在推理路线。

## 来源与验收标准

- 来源优先级：Vaswani et al. 原始 Transformer 论文；PyTorch `scaled_dot_product_attention` 官方文档和源码；FlashAttention 原始论文或官方仓库用于说明标准注意力的 IO 瓶颈。
- 内容验收：课程必须清楚区分 Q/K/V 张量、mask、softmax、复杂度、最小手算例子、工程性能路径、失败边界和自检问题。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许推送。

## 实际执行

- 核对并采用 4 个一手/官方来源：Transformer 原始论文、PyTorch SDPA 官方文档、PyTorch `torch.nn.functional` 官方源码、FlashAttention 原始论文。
- 将 `src/data/syllabus.json` 中基础模型第 2 课改为 `available`，slug 为 `transformer-attention-tensors-complexity`。
- 在 `src/data/lessons.json` 新增《Transformer：注意力的张量与复杂度》，覆盖学习目标、先修、Q/K/V 概念地图、张量流、伪代码、源码阅读顺序、工程意义、决策表、动手实验、自检问题和 4 个来源。
- 在 `src/data/articles.json` 新增对应研究长文：8 章连续解释、约 3500 个中文字符、2 个公式、最小手算例子、实现映射、4 条失败边界和 4 条结论。
- 更新 `test/content.test.mjs`，把 syllabus 校验从“每路线只能 1 节 available”改为“available 数量必须与已发布课程数据一致”。
- 更新 `scripts/verify-build.mjs`，把新增课程页纳入构建验证。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，站点回顾更新为 4 条前沿信号、7 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 12 个静态页面，包含新增课程页。
- `npm run verify:build`：9/9 页面验证通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。

## 发布结果

- 内容提交 `a562c54`（`feat: add transformer attention lesson`）已推送 `main`。
- GitHub Actions `30268939053` 成功：build job 24s，deploy job 10s。备注：Actions 输出 Node 20 deprecation annotation，但部署成功。
- 线上首页 `https://sunshine0912-love.github.io/ai-frontier-atlas/` 返回 200，并检测到“AI 前沿研究”。
- 线上前沿页 `https://sunshine0912-love.github.io/ai-frontier-atlas/frontier/` 返回 200，并检测到“信号矩阵”。
- 线上学习页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/` 返回 200，并检测到“Transformer：注意力的张量与复杂度”。
- 新增课程页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/transformer-attention-tensors-complexity/` 返回 200，并检测到“缩放点积注意力”。

## 下一方向

- 下一轮学习班次优先补多模态路线第 2 课《视觉 Token：ViT 与图像离散化》，因为基础模型已有两课，多模态仍只有 CLIP，且视觉 token 是跨模态连接器、扩散模型和多模态评测的共同先修。
- 若 09:00 前沿班次新增信号，优先补非 MoE 类主题，避免开放模型/MoE 主题过度集中。
