# 2026-07-30 09:00 前沿班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；本轮严禁修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`，也不改动其 08:30 选题任务。
- Git 状态：开始时本地 `main` 与 `origin/main` 同步在 `e968fa462cacd2423876a4484586a00f5da0ffdd`，无未提交业务改动。执行 `npm run review:site` 后仅 `ops/site-review.json` 被刷新，这是本轮维护产生的记录文件变更。
- GitHub Pages：Pages 使用 workflow 部署，公开地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；最近 5 次 `Deploy AI Frontier Atlas` workflow 均为 success，最近一次成功部署为 `docs: record causal lm lesson deployment`。
- 当前站点结构：首页是 AI 研究工作台，前沿页是信号矩阵，学习页公开四阶段课程地图，方法页明确来源分级、四维评分与回顾优先流程。
- 当前数据概况：`frontier.json` 有 6 条 S 级前沿信号，最新日期为 2026-07-27；`tracks.json` 有 6 条研究路线；`syllabus.json` 有 46 节规划；`lessons.json` 和 `articles.json` 有 9 节完整课程；`ops/site-review.json` 显示 45 个直接引用。
- 最近更新：2026-07-29 09:00 新增 `nvidia-nooa-object-oriented-agent-harness` 前沿信号并部署成功；2026-07-29 21:00 新增《预训练目标：因果语言建模与数据混合》课程并部署成功。
- 重复或陈旧主题：前沿信号中 Kimi K3 与 Instella-MoE 都有 MoE/开放栈属性，NOOA 与 Agent 架构相关，Cosmos-H-Dreams 与物理 AI/视觉世界模型相关；本轮应避免继续堆叠 MoE/单模型发布，优先寻找推理系统、评测安全、数据治理或基础设施类一手进展。
- 空路线与薄弱路线：六条路线都已有至少 1 节课程，不存在完全空路线；按 `trackCoverage`，推理、Agent、评测仍为 1/N，基础、多模态、训练为 2/N。09:00 班次不补课程，但前沿信号 domain 应尽量补推理、评测、安全或训练系统缺口。
- 待复查来源：Kimi K3 仍待完整技术报告、权重与推理代码；Claude Opus 5 仍需第三方同预算任务评测；Instella-MoE 需复查技术报告与跨硬件复现；llm-d 需复查公开基准、快照开销与多集群稳定性；Cosmos-H-Dreams 需继续复查技术报告是否从 TODO 变为公开；NOOA 需跟踪第三方复现、安全沙箱和 memory subsystem 消融。

## 本轮班次与目标

- 班次：09:00 前沿班次。
- 目标：使用 agent-reach 研究过去约 12 小时，必要时扩展到 72 小时内刚出现可靠一手资料的全球 AI 进展；默认最多新增 3 条高价值前沿信号。
- 内容取舍：优先论文、官方技术报告、官方博客、源码、模型卡、评测仓库和监管文件；如果候选只有营销声明、自报榜单或缺少方法细节，不强行更新。

## 计划动作和选择理由

1. 围绕官方 AI 博客、arXiv、Hugging Face、GitHub、监管/安全文件和可靠技术源搜索 2026-07-29 至 2026-07-30 的新进展，并回看 72 小时内一手材料刚补齐的事件。
2. 对候选信号按本站标准筛选：必须能写出 `fact`、`mechanism`、`impact`、`uncertainty`、`watchNext`、`evidenceLevel`、四维评分和直接来源。
3. 若发现 1-3 条非重复高价值信号，编辑 `src/data/frontier.json`；优先补推理系统、评测安全、训练数据治理、可复现训练栈或 Agent 系统缺口。
4. 若没有足够一手证据，则不修改业务数据，只记录无更新原因、候选复查点和下一轮方向。
5. 发布前按维护手册顺序运行完整验证链，任何失败都不提交、不推送。

## 明确不做

- 不修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`。
- 不改动旧技术博客的 08:30 选题任务。
- 不新增课程，不修改页面布局、组件或样式。
- 不把媒体报道、社交传闻、未给方法的排行榜或纯营销发布写入 `frontier.json`。

## 来源与验收标准

- 来源标准：S 级直接来源优先；A 级可靠解释只用于补上下文；B 级媒体或社区只用于发现线索。
- 内容验收：新增信号必须明确区分事实、推断和小光判断，至少包含 1 个直接来源，评分不能写进事实字段。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许提交推送。

## 实际执行

- 已按维护手册先完成回顾与计划，再开始编辑 `src/data/frontier.json`。
- agent-reach 可用性：`mcporter call 'exa.web_search_exa(...)'` 仍返回 `Unknown MCP server 'exa'`；Jina Reader 仍因匿名网络信誉限制返回 401；OpenAI 正文页对普通 `curl` 返回 Cloudflare 403。因此本轮按 agent-reach 文档 fallback，使用官方 RSS、OpenClaw `web_fetch`、GitHub API、官方文档、arXiv API、Hugging Face 数据集和官网直读核对一手来源。
- 候选筛选：Google DeepMind 2026-07-29 Lyria 3.5 属于音乐生成产品更新，缺少足够技术机制与可复现材料；NVIDIA 2026-07-29 NeMo Guardrails 自托管 coding assistant 更偏教程；NVIDIA 医疗机器人仿真文章与已有 Cosmos-H-Dreams 信号重叠；arXiv 的 Relay-OPD、piR2、Desktop-Delta Bench 等候选有研究价值，但本轮未确认足够官方源码/评测仓库或独立复现材料，先列入后续复查。
- 新增 1 条前沿信号：`openai-arc-agi-3-retained-reasoning-compaction`，归入 `evaluation` domain、`Agent 评测` category，记录事实、机制、影响、不确定性、下一观察点、四维评分和 5 个直接来源。
- 新增信号的选择理由：它补足评测/Agent harness 缺口，且不重复当前 MoE/开放模型主题；OpenAI 原文直接给出 ARC-AGI-3 对照结果，官方文档可核对 RHAE 定义，Responses API 与 compaction 文档解释机制边界。
- 运行 `npm run review:site` 后刷新 `ops/site-review.json`：前沿信号从 6 条增至 7 条，课程保持 9 节，直接引用从 45 个增至 50 个，最新前沿日期变为 2026-07-29。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，站点回顾更新为 7 条前沿信号、9 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 14 个静态页面。
- `npm run verify:build`：13/13 页面验证通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。

## 发布结果

- 内容提交 `edc9079`（`feat: add arc agi harness evaluation signal`）已推送 `main`。
- GitHub Actions `30504854015` 成功：build job 25s，deploy job 9s。备注：Actions 输出 Node 20 deprecation annotation，但构建与部署均成功。
- 线上首页 `https://sunshine0912-love.github.io/ai-frontier-atlas/` 返回 200，并检测到“VERIFIED SIGNALS”计数为 7、“PRIMARY REFERENCES”计数为 50，以及新增 `ARC-AGI-3` 信号。
- 线上前沿页 `https://sunshine0912-love.github.io/ai-frontier-atlas/frontier/` 返回 200，并检测到《ARC-AGI-3 显示长程 Agent 评测已进入 Harness 敏感区》、`13.3%`、`38.3%` 和 `Agent 评测`。
- 线上学习页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/` 返回 200，并检测到“四阶段”、训练路线《预训练目标：因果语言建模与数据混合》和推理路线。
- 线上方法页 `https://sunshine0912-love.github.io/ai-frontier-atlas/method/` 返回 200，并检测到“来源、判断”和“四维评分方法”。
- 新增信号的直接来源已核对：OpenAI 官方 ARC-AGI-3 复盘通过 OpenClaw `web_fetch` 返回 200；OpenAI Responses API 设计说明、OpenAI Compaction 官方文档、ARC-AGI-3 评分方法和 Hugging Face human logs 均由 `curl` 核对返回 200。

## 下一方向

- 下一轮 21:00 学习班次优先补推理路线第 2 课《推理性能模型：TTFT、TPOT 与吞吐》，因为推理路线仍为 1/8，且它是 FlashAttention、量化、投机解码、并行推理和容量规划的共同先修。
- 下一轮 09:00 前沿班次继续复查非 MoE 类信号，重点关注推理系统、Agent 评测/安全、训练数据治理，以及本轮暂未加入的 Relay-OPD、piR2、Desktop-Delta Bench 是否补齐源码、模型卡或评测仓库。
- 持续复查 Kimi K3 技术报告/权重、Instella-MoE 技术报告、llm-d 公开基准、Cosmos-H-Dreams 技术报告和 NOOA 第三方复现。
