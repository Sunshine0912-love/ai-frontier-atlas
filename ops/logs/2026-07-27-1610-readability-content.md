# 2026-07-27 16:10 可读性与课程完整度改造

## 当前内容回顾

- 上一版已经具备研究工作台、六研究域与课程卡片，但样式表中存在大量 7–11px 文字，低对比灰色 `#8d98a8`，中文长文使用宋体栈。
- 六篇课程的数据量约 2.4–2.6KB/篇，栏目虽全，实际主要是短句数组，仍属于提纲。
- 学习页只展示六篇已发布课程，未公开 46 节规划的顺序、依赖和连载状态，读者无法判断完整学习路径。

## 本轮目标与计划

1. 以 12px 为任何文字的下限，正文 16px，研究长文 17px/1.9 行高，重新标定灰阶对比度。
2. 公开六条路线共 46 节的完整 syllabus，区分“完整文章”和“待连载”。
3. 将现有六课扩写为研究级长文：问题设定、基线、形式化、最小例子、实现映射、复杂度/系统路径、失败边界、实验、自检和来源。
4. 使用本地 KaTeX 在构建时渲染公式，不依赖外部 CDN。
5. 用自动化测试和真实桌面/移动截图锁定可读性与内容深度。

## 实际执行

- 将 `--muted` / `--faint` 调整为更高对比的 `#425166` / `#5f6e82`，所有显式字体不小于 12px，中文正文改用系统无衬线栈。
- 新增 `syllabus.json`：基础模型 8 节、多模态 6 节、训练 9 节、推理 8 节、Agent 8 节、评测 7 节，共 46 节。
- 新增 `articles.json`：六篇文章各 7 章、至少 1800 字连续技术论述、2 个公式、worked example、实现映射、至少 3 个失败边界和 4 条结论。
- 新增 `MathBlock.astro` 并本地预渲染 KaTeX；每个公式包含符号定义和直觉解释。
- 每篇课程来源从 2 条增加到 3 条，优先论文、官方源码与官方文档。
- 维护回顾脚本新增“研究长文覆盖率”和“syllabus 覆盖率”，维护手册同步采用新标准。

## 验证结果

- `npm test`：10/10 通过。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：11 个静态页面构建成功。
- `npm run verify:build`：8/8 关键页面通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- 六篇生成 HTML 均包含本地 `katex-display`，无原始 `$$` 和 KaTeX CDN。
- 1440×1000 与 390×844 截图检查：正文、辅助信息和目录均达到可读字号；公式与表格无横向破版。

## 发布结果

- 待合并到 `main` 并验证 GitHub Pages。

## 下一方向

- 按 syllabus 依赖顺序优先连载 Transformer 注意力与视觉 Token 两课。
- 每次晚间班次只发布一篇达到长文标准的课程；若资料或公式未核实，则只做补源和修订。
