# 2026-07-29 09:00 前沿班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；本轮严禁修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`，也不改动其 08:30 选题任务。
- Git 状态：开始时本地 `main` 与 `origin/main` 同步在 `3ed7c37890a08df0a3380db11847248d0a21f608`，无未提交业务改动。执行 `npm run review:site` 后仅 `ops/site-review.json` 被刷新，这是本轮维护产生的记录文件变更。
- GitHub Pages：Pages 使用 workflow 部署，公开地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`，最近 5 次 `Deploy AI Frontier Atlas` workflow 均为 success，最近一次成功部署为 `docs: record vision token deployment`。
- 当前站点结构：首页是 AI 研究工作台，前沿页是信号矩阵，学习页公开四阶段课程地图，方法页明确来源分级、四维评分与回顾优先流程。
- 当前数据概况：`frontier.json` 有 5 条 S 级前沿信号，最新日期为 2026-07-27；`tracks.json` 有 6 条研究路线；`syllabus.json` 有 46 节规划；`lessons.json` 和 `articles.json` 有 8 节完整课程；`ops/site-review.json` 显示 35 个直接引用。
- 最近更新：2026-07-28 09:00 新增 `nvidia-cosmos-h-dreams-surgical-world-model` 前沿信号并部署成功；2026-07-28 21:00 新增《视觉 Token：ViT 与图像离散化》课程并部署成功。
- 重复或陈旧主题：前沿信号仍集中在开放模型、MoE/长上下文、训练基础设施、模型经济性和物理 AI，其中 Kimi K3 与 Instella-MoE 都带 MoE/开放栈属性；本轮应优先寻找非 MoE 类的一手进展，避免堆叠同类模型发布。
- 空路线与薄弱路线：六条路线都已有至少 1 节课程，不存在完全空路线；训练、推理、Agent、评测仍为 1/N，基础模型和多模态为 2/N。09:00 班次不补课程，但前沿信号的 domain 应尽量补足评测、安全、推理系统、Agent 或可复现训练栈缺口。
- 待复查来源：Kimi K3 仍待完整技术报告、权重与推理代码；Claude Opus 5 仍需第三方同预算任务评测；Instella-MoE 需复查技术报告与跨硬件复现；llm-d 需复查公开基准、快照开销与多集群稳定性；Cosmos-H-Dreams 需继续复查技术报告是否从 TODO 变为公开。

## 本轮班次与目标

- 班次：09:00 前沿班次。
- 目标：使用 agent-reach 研究过去约 12 小时，必要时扩展到 72 小时内刚出现可靠一手资料的全球 AI 进展；默认最多新增 3 条高价值前沿信号。
- 内容取舍：优先论文、官方技术报告、官方博客、源码、模型卡、评测仓库和监管文件；如果候选只有营销声明、自报榜单或缺少方法细节，不强行更新。

## 计划动作和选择理由

1. 围绕官方 AI 博客、arXiv、Hugging Face、GitHub、监管/安全文件和可靠技术源搜索 2026-07-28 至 2026-07-29 的新进展，并回看 72 小时内一手材料刚补齐的事件。
2. 对候选信号按本站标准筛选：必须能写出 `fact`、`mechanism`、`impact`、`uncertainty`、`watchNext`、`evidenceLevel`、四维评分和直接来源。
3. 若发现 1-3 条非重复高价值信号，编辑 `src/data/frontier.json`；优先补评测、安全、推理系统、Agent、训练系统或多模态缺口。
4. 若没有足够一手证据，则不修改业务数据，只记录无更新原因、候选复查点和下一轮方向。
5. 发布前按维护手册顺序运行完整验证链，任何失败都不提交、不推送。

## 明确不做

- 不修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`。
- 不改动旧技术博客的 08:30 选题任务。
- 不新增课程，不修改页面布局、组件或样式。
- 不把媒体报道、社交传闻、未给方法的排行榜或纯营销发布写入 `frontier.json`。

## 来源与验收标准

- 来源标准：S 级直接来源优先；A 级可靠解释只用于补上下文；B 级媒体或社区只用于发现线索。
- 内容验收：新增信号必须明确区分事实、推断和小光判断，至少包含 1 个直接来源，评分不能写进事实字段。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许提交推送。

## 实际执行

- 已按维护手册先完成回顾与计划，再开始编辑 `src/data/frontier.json`。
- agent-reach 可用性：`agent-reach doctor` 命令不存在，`mcporter call 'exa.web_search_exa(...)'` 返回 `Unknown MCP server 'exa'`；OpenClaw `web_search` 也因 Tavily API key 未配置不可用；Jina Reader 因匿名网络信誉限制返回 401。因此本轮按 agent-reach 文档 fallback，使用官方 RSS、OpenClaw `web_fetch`、GitHub CLI、官方网页和 arXiv 页面直读核对一手来源。
- 候选筛选：OpenAI 2026-07-28《Scientific computing in the age of agentic AI》是有价值的科学软件现场报告，但更偏应用案例和维护观念，不作为本站前沿信号；NVIDIA 医疗机器人仿真文章与上一条 Cosmos-H-Dreams 信号主题重叠，暂不重复新增；AgentKVShift、Semalith、ProGPO、CORVUS 等 arXiv 候选有研究价值，但未确认源码/模型卡/评测仓库等复现产物，先列入后续复查。
- 新增 1 条前沿信号：`nvidia-nooa-object-oriented-agent-harness`，归入 `agent` domain、`Agent 架构` category，明确记录事实、机制、影响、不确定性、下一观察点、四维评分和 4 个直接来源。
- 直接来源包括 NVIDIA 官方技术博客、NOOA arXiv 技术报告、`NVIDIA-NeMo/labs-OO-Agents` 官方源码和 NVIDIA OpenShell 官方源码。
- 运行 `npm run review:site` 后刷新 `ops/site-review.json`：前沿信号从 5 条增至 6 条，课程保持 8 节，直接引用从 35 个增至 39 个。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，回顾完成为 6 条前沿信号、8 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 13 个静态页面。
- `npm run verify:build`：12/12 页面验证通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。

## 发布结果

- 内容提交 `1aa3701`（`feat: add nooa agent harness signal`）已推送 `main`。
- GitHub Actions `30413351216` 成功：build job 21s，deploy job 9s。备注：Actions 输出 Node 20 deprecation annotation，但构建与部署均成功。
- 线上首页 `https://sunshine0912-love.github.io/ai-frontier-atlas/` 返回 200，并检测到“VERIFIED SIGNALS”和“NOOA”。
- 线上前沿页 `https://sunshine0912-love.github.io/ai-frontier-atlas/frontier/` 返回 200，并检测到“NOOA 把 Agent Harness”。
- 线上学习页 `https://sunshine0912-love.github.io/ai-frontier-atlas/learn/` 返回 200，并检测到“四阶段”和《视觉 Token：ViT 与图像离散化》。
- 线上方法页 `https://sunshine0912-love.github.io/ai-frontier-atlas/method/` 返回 200，并检测到“来源、判断”和“四维评分”。
- 新增信号的 4 个直接来源均返回 200：NVIDIA 官方技术博客、NOOA arXiv 技术报告、NOOA 官方源码和 NVIDIA OpenShell 官方源码。

## 下一方向

- 下一轮 21:00 学习班次优先补训练路线第 2 课《预训练目标：因果语言建模与数据混合》，因为训练路线仍为 1/9，且它是优化器、数据并行、SFT、偏好学习和训练诊断的共同先修。
- 下一轮 09:00 前沿班次继续复查非 MoE 类信号，重点关注 Agent 评测/安全、推理系统、可复现训练栈，以及本轮暂未加入的 AgentKVShift、Semalith、ProGPO、CORVUS 是否公开源码、模型卡或评测仓库。
- 持续复查 Kimi K3 技术报告/权重、Instella-MoE 技术报告、llm-d 公开基准与 Cosmos-H-Dreams 技术报告状态。
