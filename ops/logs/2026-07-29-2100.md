# 2026-07-29 21:00 学习班次

## 当前内容回顾

- 仓库边界确认：工作目录为 `/home/xujiaz/ai-frontier-atlas`，远端为 `Sunshine0912-love/ai-frontier-atlas`，线上地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；本轮严禁修改、提交、推送或部署 `/home/xujiaz/xiaoguang-blog`，也不改动其 08:30 选题任务。
- Git 状态：开始时本地 `main` 与 `origin/main` 同步在 `da52ad924537720d0d4f109d22dc0f9953b9b3e7`，无未提交业务改动。执行 `npm run review:site` 后仅 `ops/site-review.json` 被刷新，这是本轮维护产生的记录文件变更。
- GitHub Pages：Pages 使用 workflow 部署，公开地址为 `https://sunshine0912-love.github.io/ai-frontier-atlas/`；最近 5 次 `Deploy AI Frontier Atlas` workflow 均为 success，最近一次成功部署为 `docs: record nooa signal deployment`。
- 当前站点结构：首页是 AI 研究工作台，前沿页是信号矩阵，学习页公开四阶段课程地图，方法页明确来源分级、四维评分与回顾优先流程。
- 当前数据概况：`frontier.json` 有 6 条 S 级前沿信号，最新日期为 2026-07-27；`tracks.json` 有 6 条研究路线；`syllabus.json` 有 46 节规划；`lessons.json` 和 `articles.json` 有 8 节完整课程；`ops/site-review.json` 显示 39 个直接引用。
- 最近更新：2026-07-29 09:00 新增 `nvidia-nooa-object-oriented-agent-harness` 前沿信号并部署成功；2026-07-28 21:00 新增《视觉 Token：ViT 与图像离散化》课程并部署成功。
- 重复或陈旧主题：前沿信号中 Kimi K3 与 Instella-MoE 都有 MoE/开放栈属性，Cosmos-H-Dreams 与物理 AI/视觉世界模型相关；课程中基础模型和多模态已各有 2 节，继续补这两条路线会加重前段路线倾斜。
- 空路线与薄弱路线：六条路线都已有至少 1 节课程，不存在完全空路线；训练、推理、Agent、评测均为 1/N。按知识依赖，训练路线第 2 课《预训练目标：因果语言建模与数据混合》应排在优化器、混合精度、并行训练、SFT 与训练诊断之前。
- 待复查来源：Kimi K3 仍待完整技术报告、权重与推理代码；Claude Opus 5 仍需第三方同预算任务评测；Instella-MoE 需复查技术报告与跨硬件复现；llm-d 需复查公开基准、快照开销与多集群稳定性；Cosmos-H-Dreams 需继续复查技术报告是否从 TODO 变为公开；NOOA 需跟踪第三方复现、安全沙箱和 memory subsystem 消融。

## 本轮班次与目标

- 班次：21:00 学习班次。
- 目标：根据 trackCoverage 和知识依赖推进 1 节完整课程，选择训练路线第 2 课《预训练目标：因果语言建模与数据混合》。

## 计划动作和选择理由

1. 核对因果语言建模、数据混合、规模律和训练数据治理的一手来源，优先采用 GPT-3、Chinchilla、The Pile、Llama 2 / Llama 3 模型卡或技术报告等可回溯材料。
2. 在 `src/data/syllabus.json` 中为训练路线第 2 课补 slug，并将状态从 `planned` 改为 `available`，不改变后续课程顺序。
3. 在 `src/data/lessons.json` 新增结构化课程，覆盖学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、决策表、动手实验、自检问题和至少 3 个可靠来源。
4. 在 `src/data/articles.json` 新增对应长文，正文不少于 1800 个中文字符，包含至少 2 个公式、最小例子、源码路径、复杂度或性能分析、工程意义和原始来源。
5. 按维护手册完整验证链测试；任何失败都不提交、不推送。

## 明确不做

- 不新增 09:00 前沿信号，不追逐热搜。
- 不修改 `/home/xujiaz/xiaoguang-blog`、其自动任务或旧技术博客内容。
- 不重做视觉风格，不改动与课程发布无关的页面、组件或样式。
- 不提前发布训练路线后续优化器、并行训练、SFT 或训练诊断课程；本轮只补预训练目标与数据混合基础。

## 来源与验收标准

- 来源优先级：因果语言建模与 GPT-3 原始论文；Chinchilla scaling laws；The Pile 数据集论文；Llama 2 / Llama 3 官方模型卡或技术报告；必要时补充 Megatron-LM、Transformers 或 PyTorch 源码/文档解释训练数据路径。
- 内容验收：必须包含概念地图、代码脉络、工程意义、自检问题和原始来源；必须明确区分 next-token loss、tokenization、packing、data mixture、sampling weight、deduplication、contamination 与训练 compute tradeoff。
- 工程验收：依次通过 `npm test`、`npm run review:site`、`npm run check`、`npm run build`、`npm run verify:build`、`npm audit --audit-level=high`、`git diff --check` 后才允许提交推送。

## 实际执行

- 已按维护手册先完成回顾与计划，再开始编辑 `src/data/`。
- 使用 agent-reach 的 web/dev 路由核对来源：GPT-3 原始论文、Chinchilla 计算最优训练论文、The Pile 数据集论文、Llama 2 技术报告、NVIDIA Megatron Core `GPTDataset` 官方源码和 Hugging Face Transformers `DataCollatorForLanguageModeling` 官方源码均可访问。
- 将 `src/data/syllabus.json` 中训练路线第 2 课改为 `available`，slug 为 `causal-lm-objective-data-mixture`，未改变后续课程顺序。
- 在 `src/data/lessons.json` 新增《预训练目标：因果语言建模与数据混合》，覆盖学习目标、先修、概念地图、代码脉络、张量路径、伪代码、工程意义、决策表、动手实验、自检问题和 6 个来源。
- 在 `src/data/articles.json` 新增对应研究长文：7 章连续解释、3 个公式、最小 packed CLM 样本例子、实现映射、4 条失败边界和 4 条结论。
- 运行 `npm run review:site` 后刷新 `ops/site-review.json`：前沿信号保持 6 条，课程从 8 节增至 9 节，直接引用从 39 个增至 45 个。

## 验证结果

- `npm test`：10/10 通过。
- `npm run review:site`：通过，站点回顾更新为 6 条前沿信号、9 节课程。
- `npm run check`：20 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build`：成功生成 14 个静态页面，包含新增课程页。
- `npm run verify:build`：13/13 页面验证通过，动态覆盖首页、分区页和 9 个课程页。
- `npm audit --audit-level=high`：0 vulnerabilities。
- `git diff --check`：通过，无空白错误。

## 发布结果

待提交、推送和线上复核后补齐。

## 下一方向

待部署完成后补齐。
